# specify path to archimed.conf or leave blank
# default is the same as ArchimedCore
archimed_conf=/root/.archimedcore/archimed.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
